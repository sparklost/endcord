Add %filler to message format strings
    split format_message and format_newline on %filler
    update limiting for messages
        calculate post_content_len
        include it in splitting content
        after splitting append part to text before its added to chat
    update limiting for format_reply, format_reactions, format_interaction

Voice channels
    add voice channels to tree
    show voice channel activity
    join call from tree
    join call with goto
    handle multiple speaking at the same time

Voice calls
    fix playing sound
    start recorder thread, record, encode, encrypt, pack, send, stop when leave call, leave call on error
    pause recorder when muted
    send and receive RTCP?
    send speaking event?

Add flatpack
Optimize formatter by putting it in class
Downlaod 50 messages by default, but do incremental download, and draw on 25 then on 50
Optimize self.guilds by using dicts, each guild/channel is keyed with its id; if its worth it
Optimize main loop with queues and threading
Fix member list not updating when switching between same-guild channels
Vim-like bindings
Video calls
Localization
