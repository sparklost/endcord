Add extension API for custom gateway event handling
Add extension API for custom request in discord.py

Add right message side format string
    calculate post_content_len
    split content on it
    add special format string for main message
    after splitting append it to text
    add special format string for message newline
    after splitting newline append it to text
    same for reply, react and interaction line

Voice channels
    add voice channels to tree
    show voice channel activity
    join call from tree
    join call with goto
    handle multiple speaking at the same time

Voice calls
    fix playing sound
    start recorder thread, record, encode, encrypt, pack, send, stop when leave call, leave call on error
    pause recorder when muted
    send and receive RTCP?
    send speaking event?

Add flatpack
Optimize self.guilds by using dicts, each guild/channel is keyed with its id; if its worth it
Optimize main loop with queues and threading
Fix member list not updating when switching between same-guild channels
Vim-like bindings
Video calls
Localization
